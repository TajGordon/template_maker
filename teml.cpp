#include <bits/stdc++.h
#define F(I,J,K) for (int (I) = (J); (I) < (K); (I)++)
#define FR(I,J,K) for (int (I) = (K); (I) > (J); (I)--)
typedef long long ll;
typedef unsigned long long ull;
typedef long double ld;
using namespace std;
int main()
{
   freopen("temlin.txt", "r", stdin);
   freopen("temlout.txt", "w", stdout);
}